# OctoPOS Backend Environment Configuration

# Server
HOST=0.0.0.0
PORT=8080
ENVIRONMENT=development

# PostgreSQL
DB_HOST=localhost
DB_PORT=5432
DB_USER=octopos
DB_PASSWORD=octopos
DB_NAME=octopos
DB_SSLMODE=disable

# JWT (change this in production!)
JWT_SECRET=change-me-in-production
